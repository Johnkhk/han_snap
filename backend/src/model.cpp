#include "../include/model.h"

// Currently empty - used for future struct methods if needed
// This file may not be needed if all struct methods are implemented in the header,
// but it's good practice to create it for future use. 